// Auth-specific errors re-export shared errors for convenience
// Custom errors can be added here as the service grows
export { BadRequestError, UnauthorizedError, ForbiddenError, NotFoundError, ConflictError, AppError } from '@api-gateway/shared';
